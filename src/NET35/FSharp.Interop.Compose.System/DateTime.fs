// Generated with FSharp.Interop.Compose (1.14.0) http://jbtule.github.io/FSharp.Interop.Compose

namespace FSharp.Interop.Compose.System
/// Corresponding static methods as functions for [`System.DateTime`](https://docs.microsoft.com/en-us/dotnet/api/system.datetime)
module DateTime =

    /// Calls [`Add(value)`](https://docs.microsoft.com/en-us/dotnet/api/system.datetime.add)
    let inline add value (``{instance}``:System.DateTime) = ``{instance}``.Add(value)

    /// Calls [`AddDays(value)`](https://docs.microsoft.com/en-us/dotnet/api/system.datetime.adddays)
    let inline addDays value (``{instance}``:System.DateTime) = ``{instance}``.AddDays(value)

    /// Calls [`AddHours(value)`](https://docs.microsoft.com/en-us/dotnet/api/system.datetime.addhours)
    let inline addHours value (``{instance}``:System.DateTime) = ``{instance}``.AddHours(value)

    /// Calls [`AddMinutes(value)`](https://docs.microsoft.com/en-us/dotnet/api/system.datetime.addminutes)
    let inline addMinutes value (``{instance}``:System.DateTime) = ``{instance}``.AddMinutes(value)

    /// Calls [`AddMonths(months)`](https://docs.microsoft.com/en-us/dotnet/api/system.datetime.addmonths)
    let inline addMonths months (``{instance}``:System.DateTime) = ``{instance}``.AddMonths(months)

    /// Calls [`AddSeconds(value)`](https://docs.microsoft.com/en-us/dotnet/api/system.datetime.addseconds)
    let inline addSeconds value (``{instance}``:System.DateTime) = ``{instance}``.AddSeconds(value)

    /// Calls [`AddTicks(value)`](https://docs.microsoft.com/en-us/dotnet/api/system.datetime.addticks)
    let inline addTicks value (``{instance}``:System.DateTime) = ``{instance}``.AddTicks(value)

    /// Calls [`ToShortDateString()`](https://docs.microsoft.com/en-us/dotnet/api/system.datetime.toshortdatestring)
    let inline toShortDateString  (``{instance}``:System.DateTime) = ``{instance}``.ToShortDateString()

    /// Calls [`ToShortTimeString()`](https://docs.microsoft.com/en-us/dotnet/api/system.datetime.toshorttimestring)
    let inline toShortTimeString  (``{instance}``:System.DateTime) = ``{instance}``.ToShortTimeString()

    /// Calls [`ToUniversalTime()`](https://docs.microsoft.com/en-us/dotnet/api/system.datetime.touniversaltime)
    let inline toUniversalTime  (``{instance}``:System.DateTime) = ``{instance}``.ToUniversalTime()
